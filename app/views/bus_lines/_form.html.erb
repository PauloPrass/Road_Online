<%= form_with(model: bus_line) do |form| %>
  <% if bus_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bus_line.errors.count, "error") %> prohibited this bus_line from being saved:</h2>

      <ul>
        <% bus_line.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
