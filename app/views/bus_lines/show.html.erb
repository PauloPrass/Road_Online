<p id="notice"><%= notice %></p>

<div class="container">

  <div class="container_title">
    <div class="div_title_i">
      <h1>Linha</h1>
    </div>
  </div>

  <p>
    <strong>Code:</strong>
    <%= @bus_line.code %>

    <strong>Name:</strong>
    <%= @bus_line.name %>
  </p>

  <div class="container_title">
      <div class="div_title_i">
          <h1>Itinerários</h1>
      </div>
  </div>

  <br>

  <table class="table table-dark">
    <thead>
      <tr>
        <th>Latitude</th>
        <th>Longuitude</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @bus_line.itineraries.each do |itinerary| %>
        <tr>
          <td><%= itinerary.latitude %></td>
          <td><%= itinerary.longuitude %></td>
          <td><%= link_to 'Visualizar', itinerary %></td>
          <td><%= link_to 'Editar', edit_itinerary_path(itinerary) %></td>
          <td><%= link_to 'Excluir', itinerary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Edit', edit_bus_line_path(@bus_line) %> |
  <%= link_to 'Back', bus_lines_path %>

  <br>
</div>